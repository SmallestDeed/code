<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sandu.service.income.dao.CompanyDesignPlanIncomeAggregatedDao">

    <select id="getAggrgatedByCompanyId" resultType="com.sandu.api.income.model.CompanyDesignPlanIncomeAggregated">
        SELECT
          *
        FROM
          company_design_plan_income_aggregated
          WHERE
          company_id =  #{companyId} AND
          is_deleted = 0
    </select>

    <update id="updateTransferDubi">
        UPDATE
          company_design_plan_income_aggregated
          SET
          current_dubi = current_dubi - #{dubi},
          transfer_dubi = transfer_dubi + #{dubi}
          WHERE
          company_id = #{companyId} AND
          is_deleted = 0
    </update>

    <update id="updateWithdrawDubi">
        UPDATE
          company_design_plan_income_aggregated
          SET
          current_dubi = current_dubi - #{dubi},
          frozen_dubi = frozen_dubi + #{dubi}
          WHERE
          company_id = #{companyId} AND
          is_deleted = 0
    </update>

</mapper>