package com.sandu.util;

public enum FileType {
    JSON, TXT, JPG, PNG, GIF, ZIP, LF, PPNG;

    @Override
    public String toString() {
        return Utils._DOT + super.toString().toLowerCase();
    }
}
