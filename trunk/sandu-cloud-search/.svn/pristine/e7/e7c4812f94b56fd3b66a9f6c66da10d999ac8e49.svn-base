import com.sandu.search.SearchProvider;
import com.sandu.search.entity.elasticsearch.po.ProductPo;
import com.sandu.search.exception.CategoryProductIndexException;
import com.sandu.search.service.index.CategoryProductIndexService;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;

import java.util.List;

/**
 * 分类产品索引服务测试类
 *
 * @date 20171213
 * @auth pengxuangang
 */
@RunWith(SpringJUnit4ClassRunner.class)
@SpringBootTest(classes = SearchProvider.class)
public class CategoryProductIndexServiceTest {

    @Autowired
    private CategoryProductIndexService categoryProductIndexService;

    @Test
    public void getProductPOListTest() throws CategoryProductIndexException{
        List<ProductPo> productPoList = categoryProductIndexService.queryProductPoList(0, 0);
        String a = "";
    }
}
