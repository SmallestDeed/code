package com.sandu.matadata.enums;

/***
 * 排序方式
 * @author Administrator
 *
 */
public enum SortType {
	/***
	 * 升序
	 */
	Ascend(0),
	/***
	 * 降序
	 */
	Descend(1);
	private int value = 0;

	private SortType(int value) {
		this.value = value;
	}

	public int value() {
		return this.value;
	}
}
