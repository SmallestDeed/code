<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sandu.resource.dao.ResFileDao">

    <select id="getById" resultType="com.sandu.resource.model.ResFile" >
        SELECT * FROM res_file
        where id = #{id,jdbcType=INTEGER}
        and is_deleted = 0
    </select>

</mapper>