# 三度空间后台系统

[![pipeline status](http://git.sanduspace.cn/sandu/sandu-yun/badges/dev/pipeline.svg)](http://git.sanduspace.cn/sandu/sandu-yun/commits/dev)
[![coverage report](http://git.sanduspace.cn/sandu/sandu-yun/badges/dev/coverage.svg)](http://git.sanduspace.cn/sandu/sandu-yun/commits/dev)


## 端口使用说明
### Provider
- Cache         `21010`
- House         `21011`
- DesignPlan    `21012`
- Product       `21013`
- System        `21014`
- usermanager   `21015`
### Web
- PcWebAPI      `30010`
- MobileWebAPI  `30011`


## Git 说明
### 设置 git 签名
```bash
git config --global user.name "Your name"
git config --global user.email "your@email.com"
``` 

### 克隆项目
```bash
git clone git@gitlab.sanduspace.cn:sandu/sandu-yun.git
cd sandu-yun
```

### 添加远程仓库
```bash
git remote add origin git@gitlab.sanduspace.cn:sandu/sandu-yun.git
git push -u origin --all
git push -u origin --tags
```

## 工作流
1. 切换dev分支 `git checkout –b dev`
2. write code
3. 准备合并代码，先暂存代码 `git  stash`
4. 拉取远程仓库代码 `git pull origin dev`
5. 合并代码，恢复暂存 `git stash apply`
6. 提交修改
   ```
   git add .
   git commit –m “修改内容描述”
   ```
7. 推到远程仓库 `git push origin dev`



