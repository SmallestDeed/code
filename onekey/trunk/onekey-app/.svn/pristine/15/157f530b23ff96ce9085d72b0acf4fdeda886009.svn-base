package com.nork.onekeydesign.service;

import com.nork.onekeydesign.model.DesignTemplet;

/**
 * DesignTemplet模块service
 * copy from 通用版本
 * @author huangsongbo
 *
 */
public interface DesignTempletService {

	/**
	 * 通过样板房id获取样板房信息
	 * 
	 * @param designTempletId
	 * @return
	 */
	DesignTemplet get(Integer designTempletId);

	DesignTemplet getForOneKey(Integer designTempletId);

}
