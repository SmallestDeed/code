#供求信息表添加内容字段
use app_online_30;
ALTER TABLE `base_supply_demand`
MODIFY COLUMN `description`  varchar(4000) CHARACTER SET utf8 COLLATE utf8_general_ci NULL COMMENT '信息描述' AFTER `title`,
ADD COLUMN `content`  text NULL COMMENT '供求信息内容（可文字图片按格式存版）' AFTER `recommended_time`;

#用户评论表添加内容字段
use app_online_30;
ALTER TABLE `user_reviews`
ADD COLUMN `content`  text NULL COMMENT '评论内容(可文字图片按格式存)' AFTER `is_top`;