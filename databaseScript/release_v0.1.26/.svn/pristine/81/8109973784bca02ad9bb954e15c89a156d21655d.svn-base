#供求信息表添加内容字段
use app_online_30;
ALTER TABLE `base_supply_demand`
MODIFY COLUMN `description` varchar(4000) NOT NULL DEFAULT '' COMMENT '信息描述' AFTER `title`,
ADD COLUMN `content` text NOT NULL DEFAULT '' COMMENT '供求信息内容(可文字图片按格式存字段)' AFTER `recommended_time`;

#用户评论表添加内容字段
use app_online_30;
ALTER TABLE `user_reviews`
ADD COLUMN `content` text NOT NULL DEFAULT '' COMMENT '评论内容(可文字图片按格式存)' AFTER `is_top`;