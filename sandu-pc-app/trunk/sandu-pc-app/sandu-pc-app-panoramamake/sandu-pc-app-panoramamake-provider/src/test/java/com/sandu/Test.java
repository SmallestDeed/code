/*
package com.sandu;

import com.sandu.panorama.model.DesignPlanStoreRelease;
import com.sandu.panorama.service.DesignPlanStoreReleaseService;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.junit4.SpringRunner;

@RunWith(SpringRunner.class)
@SpringBootTest(webEnvironment = SpringBootTest.WebEnvironment.RANDOM_PORT)
public class Test {

    @Autowired
    private DesignPlanStoreReleaseService designPlanStoreReleaseService;

    @org.junit.Test
    public void test(){
        DesignPlanStoreRelease a = designPlanStoreReleaseService.get(196);
        System.out.println(a.toString());
    }

}

*/
